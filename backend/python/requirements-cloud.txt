# Cloud (Render native Node) — CPU wheels only, no CUDA.
# Installed during build via scripts/install-python-deps.js
--extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.26,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
onnxruntime>=1.16,<2
joblib>=1.3,<2
torch>=2.1,<3
